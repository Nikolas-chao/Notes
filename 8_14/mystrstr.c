#include <stdio.h>

int mystrstr(char *dest, char *str);
int main()
{
    char str[30] = {"hello world hello world"};
    char dest[5] = {"world"};
    int flag = mystrstr(dest, str);
    printf("%d\n", flag);
    return 0;
}
int mystrstr(char *dest, char *str)
{
  
    int *head = 0;

   
    return head;
}